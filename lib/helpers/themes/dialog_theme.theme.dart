import 'package:flutter/material.dart';

class TDialogTheme {
  static DialogTheme lightTheme = const DialogTheme(
    backgroundColor: Colors.white,
  );
  static DialogTheme darkTheme = const DialogTheme(
    backgroundColor: Color(0xFF171717),
  );
}
